# Informe del repositorio — 2026-09

**Generado por `npm run informe` el 2026-09-20.** No tiene autoridad:
describe el estado en esta fecha y se regenera. Lo que obliga está en
`docs/normativo/`; lo que es verdad hoy, en `ESTADO-ACTUAL.md`.

- **Rama:** `feature/uis-pendientes` · **HEAD:** `d921485`
- **Commits en el mes:** 97
- **Campaña en curso:** cerrar las UIs pendientes del Océano.

---

## 1 · Quién vigila cada principio

De los **16 invariantes** de `filosofia.estructural`, **3** tienen una regla que
falla si se violan, **1** tiene cobertura parcial, y los **12** restantes se sostienen porque
alguien se acuerda en la revisión — y esa es la lectura importante de esta
tabla, no la columna de la derecha en verde.

| § | Invariante | Lo vigila | Hoy |
|---|---|---|---|
| §1 | Un bloque se reemplaza, desactiva o evoluciona sin obligar a tocar código no relacionado. | **nadie** | — |
| §2 | Un módulo tiene fuente de datos propia, dependencias explícitas y contrato de salida estable. | `C5` + `C1` | 0/0 ok |
| §3 | Un módulo no lee datos de otro para adivinar información que ya tiene una fuente oficial. | **nadie** | — |
| §4 | Ningún módulo crea una segunda fuente «porque es más fácil» ni inventa datos de otro módulo. | **nadie** | — |
| §5 | La identidad académica se obtiene exclusivamente del catálogo: nunca de ETIQUETAS PERSONALES. | **nadie** | — |
| §6 | Un campo con significado fijo vive en los datos personales, nunca como etiqueta. | **nadie** | — |
| §7 | Ocultar un botón no es autorización: toda escritura valida sesión, rol y alumno en el servidor. | `C3` + `test-auditoria-permisos.mjs` | 0/0 ok |
| §8 | La UI no accede a detalles internos de otro módulo: llama Server Actions, dominio o RPC. | `C8` + `C4` | 0/0 ok |
| §9 | Toda migración es idempotente y no borra el legacy en el mismo paso: primero se verifica. | **nadie** | — |
| §10 | Se añaden claves antes que renombrarlas; si un contrato cambia, migran sus consumidores. | **nadie** | — |
| §11 | Antes de crear una consulta se revisa la existente, y se agrupa en lote, nunca en bucles 1×N. | **nadie** | — |
| §12 | La importación reutiliza lectura y mapeo, y valida todo antes de escribir. | **nadie** | — |
| §13 | Un flag de desactivación no reemplaza la autorización: si el módulo escribe, la action valida. | **nadie** | — |
| §14 | El código viejo se marca @deprecated con su alternativa; un fallback de identidad no se amplía. | **nadie** | — |
| §15 | Los documentos no se duplican entre sí: el estado lo dice ESTADO-ACTUAL.md, no este archivo. | `verificar-docs.mjs` (parcial) | — |
| §16 | No se optimiza una capa si el costo es de otra: primero se mide la capa responsable. | **nadie** | — |

Por qué cada uno está donde está:

- **§1** — C9 (ningún archivo > 1000 líneas) es un proxy del tamaño, no una prueba de que algo sea reemplazable
- **§2** — solo para los módulos `-puro`: C5 exige cero I/O y C1 imports relativos, que es lo que permite cargarlos aislados. Un módulo NO puro puede incumplir §2 sin que falle nada
- **§3** — C2 (lib/ no importa app/) detecta el acoplamiento hacia arriba; «leer datos de otro módulo para adivinar» no lo ve nadie
- **§4** — que un dato tenga UNA fuente no lo puede ver un grep: se revisa a mano (R6)
- **§5** — «la identidad académica viene del catálogo» no tiene detector; lo sostienen el GLOSARIO y la revisión
- **§6** — campo fijo vs etiqueta es semántico: ninguna comprobación lo distingue
- **§7** — una action sin `exigir()` hace fallar la auditoría, y un cliente que importe supabase hace fallar C3. Violar el invariante sí rompe algo
- **§8** — si la action habla con Supabase o llama a otra action, falla. Es la forma comprobable de «llama contratos, no tablas»
- **§9** — la idempotencia de un `.sql` no se comprueba desde disco; requiere red (punto ciego declarado del panel)
- **§10** — «aditivo antes que renombrar» solo se ve en la revisión del diff
- **§11** — no hay medición de rendimiento vigente: las cifras son de la FASE 10, anteriores al rediseño entero
- **§12** — los importadores de CSV/Excel no tienen detector de reutilización: nada impide escribir un mapeo de columnas nuevo en paralelo
- **§13** — que un flag no sustituya a la autorización lo roza la auditoría de permisos, no lo cubre
- **§14** — nadie comprueba que el legacy lleve `@deprecated` con su alternativa
- **§15** — desde 2026-09-19 vigila rutas vivas y el techo de arranque; la NO duplicación entre documentos sigue sin detector
- **§16** — «medir la capa responsable» es método, no estado: no hay nada que comprobar

---

## 2 · El hilo, en código

Los números de arriba dicen que nadie viola las reglas. Esto enseña cómo se ve
cumplirlas: archivos reales del repo de hoy, elegidos por el propio script —no
escogidos a mano, para que el informe no mienta el día que uno se mueva—.

### §8 · La action valida y delega; no hace el trabajo

`app/actions/tablas.ts`

```ts
 3  import { exigir } from "@/lib/auth/exigir";
14    const g = await exigir("calificacion.ver");
16    return listarRegistrosCompletos();
```

Ni una consulta: `exigir()` decide si puede, y el trabajo vive en `lib/`. Por eso C8 está en 0.

### §2 · La decisión vive en un módulo puro, probable sin base de datos

`lib/escolar/ciclo/ciclo-estado-puro.ts`

```ts
 32  export function esEstadoCiclo(valor: unknown): valor is EstadoCiclo {
 40  export function estadoDesdeActivo(activo: boolean): EstadoCiclo {
 54  export function resolverEstadoPeriodo(fila: FilaPeriodoEstado): EstadoCiclo {
 99  export function validarIntegridadCiclo(
```

Cero `import` de Supabase en todo el archivo: se puede correr su suite sin red. Es lo que mide C5.

### §2 · …y se puede cargar aislado: el dominio importa por ruta relativa

`lib/escolar/materia/materia-avance.ts`

```ts
23  import { filaCoincideAlumno, type CriterioAlumnoEnFila } from "../buscar-en-filas";
24  import { archivoCsvAFilas } from "../csv";
25  import { matrizAFilasDirectas } from "../excel-a-registros";
26  import { obtenerMapeoColumnasMateria } from "./mapeo-columnas-materia";
```

Nada de `@/`. No es estética: con el alias, las suites puras dejan de transpilar y el build sigue verde — el fallo se descubre tarde. Es C1.

---

## 3 · Qué se movió

Entre la primera y la última medición del panel (`659300a` → `d921485`,
22 mediciones):

| Señal | Antes | Ahora | |
|---|---|---|---|
| `orden.C8` | 13 | 0 | -13 |
| `orden.C9` | 7 | 0 | -7 |
| `tecnico.suites` | 37 | 39 | +2 |
| `frontend.restyle` | 69 | 97 | +28 |
| `frontend.claro` | 711 | 52 | -659 |
| `datos.sql` | 45 | 47 | +2 |
| `docs.frescura` | 2 | 1 | -1 |
| `docs.suitesDeclaradas` | 37 | 39 | +2 |
| `docs.lineas` | 264 | 138 | -126 |
| `docs.arranque` | 10699 | 9946 | -753 |
| `docs.peso` | 744 | 882 | +138 |
| `pendientes.total` | 10 | 12 | +2 |

El signo no dice si es bueno: `frontend.claro` bajando es avance, y
`docs.peso` subiendo no siempre es malo. El panel sí lo sabe (`peorSi`).

---

## 4 · Lo que sigue abierto

**12 pendientes**, 3 de riesgo alto. Fuente: `docs/sistema/pendientes.json`.

- **asignaciones_profesor sigue con 0 filas** — riesgo alto, lo cierra una **persona**, revisado 2026-09-16
  - comprobar: `node scripts/diag-asignaciones-profesor.mjs`
- **15 de 21 profesores comparten contraseña** — riesgo alto, lo cierra una **persona**, revisado 2026-09-16
  - comprobar: `node scripts/diag-credenciales-duplicadas.mjs`
- **Rotar la contraseña de Supabase** — riesgo alto, lo cierra una **persona**, revisado 2026-09-16
- **O-1: la RPC de perfil de alumno no acota por grupo** — riesgo medio, lo cierra un **agente**, revisado 2026-09-16
  - comprobar: `node scripts/diag-sql-aplicado.mjs`
- **calendario_escolar.periodo_id sigue sin FK** — riesgo medio, lo cierra un **agente**, revisado 2026-09-16
  - comprobar: `node scripts/diag-relaciones-supabase.mjs`
- **Re-medir rendimiento (FASE 11)** — riesgo medio, lo cierra un **agente**, revisado 2026-09-16
- **68 de 3863 filas de asistencia_alumnos sin periodo_id** — riesgo bajo, lo cierra una **persona**, revisado 2026-09-16
  - comprobar: `node scripts/diag-asistencia-periodo.mjs`
- **agregar-indices-asistencia.sql sin verificar** — riesgo bajo, lo cierra una **persona**, revisado 2026-09-16
- **PROMPT-5 Parte B: queda B4** — riesgo bajo, lo cierra un **agente**, revisado 2026-09-16
  - comprobar: `node scripts/test-orden.mjs --detalle`
- **PROMPT-4/T4 sin estrenar sobre datos reales** — riesgo medio, lo cierra una **persona**, revisado 2026-09-16
- **agregar-grupo-materia-justificaciones.sql sin aplicar** — riesgo medio, lo cierra una **persona**, revisado 2026-09-17
  - comprobar: `node scripts/diag-sql-aplicado.mjs`
- **MATRIZ-UX §§2, 3, 5 y 6 describen la UI retirada en 8d17188** — riesgo medio, lo cierra un **agente**, revisado 2026-09-19
  - comprobar: `node scripts/verificar-docs.mjs`

---

## 5 · Dónde no hay nadie mirando

**12 de los 16 invariantes no tienen detector.** Si uno se rompe hoy, se
descubre cuando falle algo en producción o cuando alguien lo lea en una revisión:

- §1 — Un bloque se reemplaza, desactiva o evoluciona sin obligar a tocar código no relacionado.
- §3 — Un módulo no lee datos de otro para adivinar información que ya tiene una fuente oficial.
- §4 — Ningún módulo crea una segunda fuente «porque es más fácil» ni inventa datos de otro módulo.
- §5 — La identidad académica se obtiene exclusivamente del catálogo: nunca de ETIQUETAS PERSONALES.
- §6 — Un campo con significado fijo vive en los datos personales, nunca como etiqueta.
- §9 — Toda migración es idempotente y no borra el legacy en el mismo paso: primero se verifica.
- §10 — Se añaden claves antes que renombrarlas; si un contrato cambia, migran sus consumidores.
- §11 — Antes de crear una consulta se revisa la existente, y se agrupa en lote, nunca en bucles 1×N.
- §12 — La importación reutiliza lectura y mapeo, y valida todo antes de escribir.
- §13 — Un flag de desactivación no reemplaza la autorización: si el módulo escribe, la action valida.
- §14 — El código viejo se marca @deprecated con su alternativa; un fallback de identidad no se amplía.
- §16 — No se optimiza una capa si el costo es de otra: primero se mide la capa responsable.

Además, el arranque de los agentes cuesta **9,996 tokens** de un techo de
10,500, y hay **0 rutas muertas** en los 25 documentos del presente.

Y lo que el panel declara que **no mide**:

- lint — SÍ está en el CI desde el PROMPT F y hoy pasa; lo que el panel no ve es el recuento de warnings (necesita eslint -f json)
- tsc --noEmit — no se corre en el panel rápido
- rendimiento — las cifras de FASE 10 son anteriores al rediseño Océano entero
- Supabase — filas, FK, RPC y SQL aplicado: todo requiere red (Fase 2)

Una zona en silencio entrena a creer que está bien. Por eso este apartado va al
final y no se omite cuando está vacío.

